version: '3.8'

services:
  api:
    build: .
    container_name: workflow-api
    ports:
      - "9090:9090"
    environment:
      - WORKFLOW_STORAGE_DIR=/app/workflow_storage
    volumes:
      - workflow_data:/app/workflow_storage

volumes:
  workflow_data:
